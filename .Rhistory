# skew = skew,
pos.def = T,
return.groups = F)
# eigen(cor_mat)$values
if(any(abs(cor_mat) > 1)) stop("values greater than 1...")
if(any(eigen(cor_mat)$values < -1e-8)) stop("negative eigenvalues...")
# if(sum(eigen(cor_mat)$values > 1e-8) != rank) stop("defined rank not acheived...")
}
rank
within_cor
for(i in 1:100){
print(i)
ngroups <- 3 #sample(1:10,1)
n <- sample(3:100, ngroups)
print(n)
# n[1] <- 1
# (within_cor <- sample(seq(0,1,0.001),1))
(within_cor <- sample(seq(0,1,0.001),ngroups))
# within_cor[1] <- 1
# within_cor[2] <- 1
# within_cor[3] <- 1
(between_cor <- sample(seq(0,1,0.001),1))
while(sum(within_cor < between_cor) != 0) between_cor <- sample(seq(0,1,0.001),1)
(rank <- sample(ngroups:sum(n),1))
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
# skew = skew,
pos.def = T,
return.groups = F)
# eigen(cor_mat)$values
if(any(abs(cor_mat) > 1)) stop("values greater than 1...")
if(any(eigen(cor_mat)$values < -1e-8)) stop("negative eigenvalues...")
# if(sum(eigen(cor_mat)$values > 1e-8) != rank) stop("defined rank not acheived...")
}
rank
sum(eigen(cor_mat)$values > 1e-8)
n
for(i in 1:100){
print(i)
ngroups <- sample(1:10,1)
n <- sample(3:100, ngroups)
print(n)
# n[1] <- 1
# (within_cor <- sample(seq(0,1,0.001),1))
(within_cor <- sample(seq(0,1,0.001),ngroups))
# within_cor[1] <- 1
# within_cor[2] <- 1
# within_cor[3] <- 1
(between_cor <- sample(seq(0,1,0.001),1))
while(sum(within_cor < between_cor) != 0) between_cor <- sample(seq(0,1,0.001),1)
(rank <- sample(ngroups:sum(n),1))
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
# skew = skew,
pos.def = T,
return.groups = F)
# eigen(cor_mat)$values
if(any(abs(cor_mat) > 1)) stop("values greater than 1...")
if(any(eigen(cor_mat)$values < -1e-8)) stop("negative eigenvalues...")
# if(sum(eigen(cor_mat)$values > 1e-8) != rank) stop("defined rank not acheived...")
}
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
# skew = skew,
pos.def = T,
return.groups = F)
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = -1,
# range = range,
rank = rank,
skew = 1,
pos.def = F)
print(i)
n <- sample(3:100,1)
print(n)
base_cor <- sample(seq(0,1,0.001),1)
rank <- sample(3:n,1)
if(n == 3) rank <- 3
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = -1,
# range = range,
rank = rank,
skew = 1,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = base_cor,
# range = range,
rank = rank,
skew = 1,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = base_cor,
# range = range,
rank = rank,
skew = -1,
pos.def = F)
eigen(cor_mat)$values
hist(cor_mat)
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = base_cor,
# range = range,
rank = rank,
skew = -0.9,
pos.def = F)
hist(cor_mat)
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = n,
base.cor = base_cor,
# range = range,
rank = rank,
skew = -1,
pos.def = F)
hist(cor_mat)
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = base_cor,
# range = range,
rank = rank,
skew = -1,
pos.def = F)
hist(cor_mat)
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = base_cor,
# range = range,
rank = rank,
skew = -0.5,
pos.def = F)
hist(cor_mat)
rank
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = base_cor,
# range = range,
rank = 5,
skew = -0.5,
pos.def = F)
hist(cor_mat)
base_cor
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = 0.6,
# range = range,
rank = 5,
skew = -0.5,
pos.def = F)
hist(cor_mat)
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = 0.6,
# range = range,
rank = 100,
skew = -0.5,
pos.def = F)
hist(cor_mat)
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
ngroups <- sample(1:10,1)
n <- sample(3:100, ngroups)
print(n)
# n[1] <- 1
# (within_cor <- sample(seq(0,1,0.001),1))
(within_cor <- sample(seq(0,1,0.001),ngroups))
# within_cor[1] <- 1
# within_cor[2] <- 1
# within_cor[3] <- 1
(between_cor <- sample(seq(0,1,0.001),1))
while(sum(within_cor < between_cor) != 0) between_cor <- sample(seq(0,1,0.001),1)
(rank <- sample(ngroups:sum(n),1))
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
# skew = skew,
pos.def = T,
return.groups = F)
skew <- sample(seq(-1,1,0.001),1)
(skew <- sample(seq(-1,1,0.001),1))
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = T,
return.groups = F)
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
eigen(cor_mat)$values
skew
rank
sum(eigen(cor_mat)$values > 1e-8)
print(i)
n <- sample(3:100,1)
print(n)
base_cor <- sample(seq(0,1,0.001),1)
rank <- sample(3:n,1)
if(n == 3) rank <- 3
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = 0.6,
# range = range,
rank = 100,
skew = 0.5,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 1000,
base.cor = 0.6,
# range = range,
rank = 10,
skew = 0.5,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 100,
base.cor = 0.6,
# range = range,
rank = 10,
skew = 0.5,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 100,
base.cor = 0.6,
# range = range,
rank = 10,
skew = 0.1,
pos.def = F)
eigen(cor_mat)$values
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 100,
base.cor = 0.6,
# range = range,
rank = 10,
skew = 0.05,
pos.def = F)
eigen(cor_mat)$values
skew
rank
# skew <- sample(seq(-1,1,0.001),1)
cor_mat <- struc_cor_mat(n = 100,
base.cor = 0.6,
# range = range,
rank = 3,
skew = 0.05,
pos.def = F)
eigen(cor_mat)$values
within_cor
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
ngroups <- sample(1:10,1)
n <- sample(3:100, ngroups)
print(n)
# n[1] <- 1
# (within_cor <- sample(seq(0,1,0.001),1))
(within_cor <- sample(seq(0,1,0.001),ngroups))
# within_cor[1] <- 1
# within_cor[2] <- 1
# within_cor[3] <- 1
(between_cor <- sample(seq(0,1,0.001),1))
while(sum(within_cor < between_cor) != 0) between_cor <- sample(seq(0,1,0.001),1)
(rank <- sample(ngroups:sum(n),1))
(skew <- sample(seq(-1,1,0.001),1))
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
eigen(cor_mat)$values
if(any(abs(cor_mat) > 1)) stop("values greater than 1...")
if(any(eigen(cor_mat)$values < -1e-8)) stop("negative eigenvalues...")
if(sum(eigen(cor_mat)$values > 1e-8) != rank) stop("defined rank not acheived...")
sum(eigen(cor_mat)$values > 1e-8)
rank
skew
hist(cor_mat)
within_cor
rank
hist(cor_mat)
eigen(cor_mat)$values
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
between_cor
within_cor
ngroups <- 2#sample(1:10,1)
n <- sample(3:100, ngroups)
print(n)
# n[1] <- 1
# (within_cor <- sample(seq(0,1,0.001),1))
(within_cor <- sample(seq(0,1,0.001),ngroups))
# within_cor[1] <- 1
# within_cor[2] <- 1
# within_cor[3] <- 1
(between_cor <- sample(seq(0,1,0.001),1))
while(sum(within_cor < between_cor) != 0) between_cor <- sample(seq(0,1,0.001),1)
(rank <- sample(ngroups:sum(n),1))
(skew <- sample(seq(-1,1,0.001),1))
cor_mat <- group_cor_mat(n = n,
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
hist(cor_mat)
n
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = within_cor,
between.cor = between_cor,
# range = 0.35,
rank = rank,
skew = skew,
pos.def = F,
return.groups = F)
hist(cor_mat)
between_cor
within_cor
(skew <- sample(seq(-1,1,0.001),1))
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = within_cor,
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
eigen(cor_mat)$values
within_cor
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = 0,
pos.def = F,
return.groups = F)
hist(cor_mat)
source("~/Documents/EIB/0FieldSimR/0UnderDevelopment/group_cor_mat.R")
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = 0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
eigen(cor_mat)$values
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = 0.1,
pos.def = F,
return.groups = F)
hist(cor_mat)
eigen(cor_mat)$values
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = 0.1,
pos.def = F,
return.groups = F)
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = rank,
skew = -0.5,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(100,200),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = rank,
skew = -0.5,
pos.def = F,
return.groups = F)
hist(cor_mat)
eigen(cor_mat)$values
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = rank,
skew = -0.5,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = rank,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = rank,
skew = -0,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = rank,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
rank
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.5,
# range = 0.35,
rank = 5,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
cor_mat <- group_cor_mat(n = c(1000,2000),
within.cor = c(0.5),
between.cor = 0.2,
# range = 0.35,
rank = 5,
skew = -0.8,
pos.def = F,
return.groups = F)
hist(cor_mat)
eigen(cor_mat)$values
